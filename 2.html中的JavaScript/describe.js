    /***
     *  javascript 是通过<script></script>标签插入到HTML页面中的。
     *  这个元素用于把JavaScript代码嵌入到HTML页面汇总，跟其他标记混合在一起。
     *  也可以用于引入保存在外部文件中的javascript
     * -  引入外部js文件，写在src中，文件可以跟网页在同一台服务器上，也可以位于完全不同的域
     * -  在不使用defer 和 async 属性的情况下，包含在scrpt元素中的代码必须严格按次序解析。
     */